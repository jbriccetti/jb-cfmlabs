place files in this directorty by working off the web host name and it will automatically get fired (included) to load global variables

this allows you to easily change variables by just having the code run on different servers (dev, qa, prod)

included are (3) example configurations for dev (localhost.cfm), dev (jbriccetti.local.cfm) and prod (labs.troyweb.com.cfm)


